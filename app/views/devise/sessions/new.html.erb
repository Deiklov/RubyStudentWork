<h2><%= t('devise.log_in') %></h2>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :email, t('devise.email') %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: t('devise.email') %>
  </div>

  <div class="field">
    <%= f.label :password, t('devise.password') %><br />
    <%= f.password_field :password, autocomplete: t('devise.curr_password') %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me, t('devise.remember_me') %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.submit t('devise.log_in') %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
